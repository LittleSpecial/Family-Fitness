# FamilyFit 环境变量配置示例
# 复制此文件为 .env 并填入你的配置

# 阿里云千问 API 配置 (必填)
QWEN_API_KEY=your_qwen_api_key_here

# 数据库配置 (可选,默认使用 SQLite)
# DATABASE_URL=sqlite:///./data/health.db

# 上传文件存储目录 (可选)
# UPLOAD_DIR=./uploads

# 文件大小限制 (可选,单位:字节,默认10MB)
# MAX_FILE_SIZE=10485760

# API 超时时间 (可选,单位:秒,默认30秒)
# API_TIMEOUT=30
